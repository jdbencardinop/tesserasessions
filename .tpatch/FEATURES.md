# Tracked Features

| Slug | Title | State | Compatibility |
|------|-------|-------|---------------|
| `agent-source-expansion` | Expand agent-source adapters beyond Claude and Copilot, starting with Hermes Agent and later t3code once the core inventory model is stable; keep parsers read-only and metadata-first. | applied | unknown |
| `cli-inventory-core` | Build the foundation for tss: a standalone local-first Go CLI that scans Claude Code and GitHub Copilot CLI session stores, stores normalized session metadata in SQLite, and exposes doctor, scan, list, show, attach/open/send command scaffolding. | applied | unknown |
| `codex-session-adapter` | Add a read-only, metadata-first OpenAI Codex CLI historical session adapter using authoritative active JSONL rollout files. Preserve exact thread IDs, cwd, names, timestamps, Git metadata, interactive-source filtering, custom CODEX_HOME resume context, authoritative reconciliation, synthetic fixtures, and documentation; defer archived sessions, compressed .jsonl.zst rollouts, and SQLite acceleration. | applied | unknown |
| `docs-guide-cheatsheet` | Document tss for day-to-day use: create a README plus docs/cheatsheet.md that explain scope, architecture, storage locations, commands, examples, safety/privacy defaults, and the tpatch tracking workflow. | applied | unknown |
| `live-herdr-tmux-control` | Implement robust live session control through Herdr-first and tmux-fallback adapters: discover live panes, attach/resume sessions, open new panes/windows in the same directory, send prompts, and run commands safely. | applied | unknown |
| `operator-learning-runbook` | Create a mission-driven operator learning workspace for tss, adapting the teach skill at https://github.com/mattpocock/skills/tree/main/skills/productivity/teach. Teach users to discover, inspect, curate, resume, monitor, and troubleshoot agent sessions through a concise runbook, trusted annotated resources, canonical glossary, printable quick references, short practice lessons with reusable assets, and evidence-based learning records. Ground every exercise in actual tss commands and privacy-safe synthetic data; distinguish quick reference from durable learning, and keep hosted docs, telemetry, and agent-tool tutorials out of scope. | applied | unknown |
| `repo-scaffold-consistency` | Align the tesserasessions repository scaffold with sibling Tessera Go CLIs. Compare tesserapatch/tpatch and tesseraspaces/tesseraworkspaces conventions for Makefile targets, version injection, buildinfo handling, README sections, LICENSE, .gitignore, changelog/docs files, completions, and install-from-source workflow; then add the missing project files and CLI version wiring for a consistent developer experience. | applied | unknown |
| `runtime-status-contract` | Expose a side-effect-free, versioned batch JSON status-provider contract for external orchestrators such as tws. Query live runtime observations by opaque query ID plus verified path and optional Git hints; return freshness, match evidence, raw runtimes, and separate runtime_presence and agent_state aggregates without guessing cwd-less sessions. | applied | unknown |
| `session-ux-roadmap` | Improve session UX with filters, grouping, tags, pin/done markers, stale thresholds, shell completions, and roadmap-driven workflows for managing many agent tasks. | applied | unknown |
| `smart-session-summaries` | Improve smart session summaries: generate useful local/extractive titles, goals, blockers, next actions, and confidence from transcripts and pane output; keep remote LLM summarization explicitly opt-in. | applied | unknown |
