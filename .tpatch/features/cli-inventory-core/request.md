# Feature Request: Build the foundation for tss: a standalone local-first Go CLI that scans Claude Code and GitHub Copilot CLI session stores, stores normalized session metadata in SQLite, and exposes doctor, scan, list, show, attach/open/send command scaffolding.

**Slug**: `cli-inventory-core`
**Created**: 2026-07-07T04:31:46Z

## Description

Build the foundation for tss: a standalone local-first Go CLI that scans Claude Code and GitHub Copilot CLI session stores, stores normalized session metadata in SQLite, and exposes doctor, scan, list, show, attach/open/send command scaffolding.
